{
    "name": "pc-parts",
    "version": "1.0.0",
    "scripts": {
      "start": "cd backend && npm install && npm start && cd ../frontend && npm install && npm start",
      "heroku-postbuild": "npm install --prefix frontend && npm start --prefix frontend"
    },
    "engines": {
      "node": "14.x"
    },
    "dependencies": {}
  }
  